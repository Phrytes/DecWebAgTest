// Lifted to @decwebag/sync-engine.  Folio re-exports for backwards
// compatibility with internal callers + tests that still import from here.
export { fsNode, createFsNode } from '@decwebag/sync-engine/adapters/fsNode';
