/**
 * ConfirmModal — re-export of the lifted substrate component.
 *
 * Lifted to `@decwebag/react-native/components` 2026-05-09 (Phase
 * 41.0.b B3). Substrate defaults are English (`Confirm` / `Cancel`);
 * Stoop's call sites pass localised labels via `confirmLabel` /
 * `cancelLabel`.
 */
export { ConfirmModal } from '@decwebag/react-native/components';
