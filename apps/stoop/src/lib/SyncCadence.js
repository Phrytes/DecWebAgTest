/**
 * SyncCadence — re-export shim around `@decwebag/local-store`.
 *
 * **2026-05-08:** the implementation lifted into the
 * `@decwebag/local-store` substrate (Tasks V1 = rule-of-two consumer
 * per `Project Files/Stoop/migration-tasks-v1-lifts-2026-05-08.md`).
 */

export { SyncCadence } from '@decwebag/local-store';
