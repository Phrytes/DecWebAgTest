{
  "name": "@decwebag/notify-envelope",
  "version": "0.1.0",
  "description": "Mediates persistent-content writes — per-write mode picker (envelope-only vs full-payload + queue) backed by pseudo-pod + pod-routing. Owns the pending-pod-upload queue that powers graceful degradation. Standardisation Phase 52.4.",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@decwebag/core":        "*",
    "@decwebag/pod-routing": "*",
    "@decwebag/pseudo-pod":  "*"
  },
  "devDependencies": {
    "@decwebag/core":        "file:../core",
    "@decwebag/pod-routing": "file:../pod-routing",
    "@decwebag/pseudo-pod":  "file:../pseudo-pod",
    "vitest": "^2.1.9"
  }
}
