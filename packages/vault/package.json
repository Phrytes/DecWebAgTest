{
  "name": "@decwebag/vault",
  "version": "0.1.0",
  "description": "Vault — agent identity + token storage (Vault, VaultMemory, VaultLocalStorage, VaultIndexedDB, VaultNodeFs, OAuthVault). Platform-portable: Node, browser via IndexedDB / LocalStorage, RN via the storage adapters in @decwebag/react-native. Extracted from @decwebag/core/identity 2026-05-11 as part of the standardisation P1 work (Phase 50.1.A) to fix the layering inversion that the Phase 50.1 SolidVault deprecation re-export introduced.",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@decwebag/core": "file:../core",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1",
    "vitest": "^2.1.9"
  }
}
